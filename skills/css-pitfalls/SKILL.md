---
name: css-pitfalls
description: CSS の詳細度と文字色コントラストで確実に踏む2つの罠。font-size / color を持つクラスを書く直前、およびページの文字色を決める直前に必ず起動する。2026-08-09 に1日で3回、2026-08-28 に1日で2回踏んでいる。どちらも目視レビューでは通る(症状が画面に出ない)ので、この手順を飛ばすと壊れたまま出荷される。例外なし。
---

# CSS Pitfalls

コミット前に機械で検出する。目視では通ってしまう2つの罠を扱う。

## 1. 部品クラスがユーティリティを後勝ちで潰す

部品クラス（`.card` / `.num` / `.stat .val` のような「モノ」を表すクラス）に
**font-size や color を持たせると、後から付けたユーティリティクラスを後勝ちで潰す**。
詳細度が同じ (0,1,0) なら、CSS の後ろに書いたほうが勝つ。

```css
/* WRONG: .num-lg が後ろにあるので、.disp-3 を付けても 32px のまま */
.disp-3 { font-size: 96px; }        /* 前 */
.num-lg { font-size: 32px; }        /* 後。こちらが勝つ */
```

```css
/* CORRECT: ユーティリティが勝つ行を対で書く */
.num-lg { font-size: 32px; }
.num.disp-3 { font-size: 96px; }    /* (0,2,0) で確実に勝つ */
```

**症状が出ない**のが最悪の性質。96px のはずが 32px でも「数字が小さめ」に見えるだけで、
色が消えていなければ壊れているように見えない。目視レビューでは通る。

検出は機械でやる。**クラスが指すトークンの値と、実際の `getComputedStyle().fontSize`
を突き合わせる**。期待値は CSS 変数から実行時に読む（px を書き写すと二重ソースになる）。

**痛点**: 2026-08-09 に1日で3回踏んだ。
`.stat .val` (0,2,0) が `.disp-1`(160px) を 53px に潰し、
`.num-lg` が `.disp-3`(96px) を 32px に潰し、
`.t1`/`.t2` の自前の色が濃い面のコンテナの `color` を無視して面と同色になった。
うち2件は **カタログ中1箇所でしか使われていないトークン** で起きたので、
「定義したのに一度も画面に出たことが無い」状態が続いていた。
使用箇所が1つしか無いトークンは、壊れていても誰も気づかない。

## 2. 色の合否は、その色が載る面を全部数えてから決めろ。1面で通ったは通ったではない

**文字色が基準を満たすかは、その文字が実際に載る面をすべて列挙してから判定する。例外なし。**
1つの面で測って通ったので他を測らなかった、が2回続けて起きている。

面を1つ増やしたら、**その面の上に載る文字の数だけペアが増える**。
ペア表は人が手で書くので、面を足したときに必ず追随し損ねる。
そして**追随し損ねても症状が出ない**。文字は表示される。少し読みにくいだけだ。目視レビューでは通る。

```python
# WRONG: カードの上で測って通ったので確定した
contrast(brand, surface_raised)   # 4.80  OK → 採用

# CORRECT: その色が載る面を全部数えてから決める
for face in (surface, surface_raised, surface_subtle, session_quali, session_race):
    assert contrast(fg, face) >= 4.5    # 1つでも割れたら不採用
```

**判定は最小値でやる。平均でも代表値でもない。** 5面のうち1面で割れる色は、その1面で割れる。

**痛点**: 2026-08-28 に1日で2回踏んだ。

- `precip-mid` `24 88% 36%` は `surface` / `surface-raised` / `surface-subtle` の3面で 4.77〜5.50 で通ったが、
  **セッションのタイルの上で 4.22**。降水確率が出るのはまさにそのタイルの中だった。34% に下げて最小 4.65
- `brand` `0 72% 51%` をそのまま文字に使う設計にしていた。白いカード `#FFFFFF` の上では **4.80** で通る。
  `surface` `#F6F7F9` で **4.48**、`surface-subtle` `#ECEFF3` で **4.16**。
  **通っていたのは3面のうち1面だけだった**

どちらも「面を後から増やしたのに、増やす前に測ったペア表を更新しなかった」形。
1件目のとき「ペア表を数え直す」で済ませたので、2件目が来た。

**面を足す変更は、色の変更として扱え。** 面の追加は背景を1つ増やす行為であって、
その面の上に載る文字は全部、値を確定し直す対象になる。

## 関連

- `~/.claude/rules/coding-style.md`（この skill を指すポインタが 1 行ある）
